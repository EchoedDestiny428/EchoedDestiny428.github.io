# Theme removed so custom CSS does not conflict
# Enable GitHub Pages-friendly build for additional pages.

title: EchoedDestiny428 Portfolio
description: Portfolio

# Site settings
url: "https://echoeddestiny428.github.io"
baseurl: ""

# Build settings
markdown: kramdown
plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Exclude from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/